import SuggestionNav from "../SuggestionNav";
import Layout from "./Layout";

const LayoutS = () => {
  return (
    <div>
      <Layout>
      <SuggestionNav />
      </Layout>
    </div>
  );
};

export default LayoutS;
